import { MainLayout } from "~/app"

export default function NotFound() {
  return (
    <MainLayout noFooter>
      <div class="mt-4 text-center text-md font-semibold sm:text-lg">Страница не найдена</div>
    </MainLayout>
  )
}
